import "@testing-library/jest-dom";
import { render } from "@testing-library/react";
import CancelRecipe from "./cancelrecipe";

describe("CancelRecipe", () => {
  it("renders a CancelRecipe", () => {
    render(<CancelRecipe />);
  });
});
